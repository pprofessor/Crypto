//! ماژول ریپازیتوری‌ها (Repositories).
//! این ماژول مسئول تمام تعاملات با لایه ذخیره‌سازی داده (دیتابیس) است و منطق دسترسی به داده را کپسوله می‌کند.

// تعریف یک `trait` پایه برای عملیات مشترک ریپازیتوری‌ها.
// این کار باعث می‌شود وابستگی‌ها به رابط (interface) باشند، نه پیاده‌سازی خاص، که برای تست و انعطاف‌پذیری بهتر است.
pub trait Repository {
    // در این مرحله توابع مشترک خاصی تعریف نمی‌کنیم.
    // این `trait` می‌تواند در آینده گسترش یابد.
}

// اعلان ماژول wallet_repository. فایل wallet_repository.rs را شامل می‌شود.
mod wallet_repository;
// اکسپورت عمومی ساختارها و `trait`های تعریف شده در آن فایل.
pub use wallet_repository::WalletRepository;